// Preload script for ChatGPT web wrapper
// Can be used to inject custom styles or scripts if needed

window.addEventListener('DOMContentLoaded', () => {
  // Add custom class to identify wrapped version
  document.documentElement.classList.add('chatgpt-desktop',);
},);
